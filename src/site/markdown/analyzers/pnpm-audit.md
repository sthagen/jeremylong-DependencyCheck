PNPM Audit Analyzer
================

Uses the PNPM CLI `audit` command to analyze `pnpm-lock.yaml` files and retrieve vulnerabilities from the [NPM Audit](https://www.npmjs.com/) APIs.

Files Types Scanned: package.json, pnpm-lock.yaml
